# dist/ wird bewusst NICHT ignoriert: es dient als fertig gebauter Ordner
# für einen schnellen Livetest im Browser (siehe README.md). Vor jedem Commit
# mit `npm run build` neu erzeugen, damit er nicht veraltet.
node_modules/
dev-dist/
spike/word-merge/output/
*.tsbuildinfo
*.local
